/**
 * ${componentName}
 * @author ${username}
 * @date ${curDate}
 */

import Vue from '@/components/base'
import App from './${componentName}'
import '@/style/app.scss'

new Vue({
    render: h => h(App)
}).$mount('#app')
